{% extends 'base.html.twig' %}

{% block title %}Mes fiches{% endblock %}

{% block body %}

{% for f in fiches %}

<tr>
    <!-- <td>{{ f.id.autheur }}</td> -->
    <td>
        <!-- <a href="{{ path('LIEN VERS LA FICHE', {'id': f.id.autheur}) }}" title="Voir cette fiche">{{ f.titre }}</a> -->
    </td>
    <td>{{ a.date|date('d/m/Y') }}</td>

    <td>
        <a href="{{ path('modifier_article', {'id': a.id}) }}" title="Modifier cet article" class="btn btn-primary">
            <i class="fas fa-edit"></i>
        </a>
        <a href="{{ path( 'supprimer_article', {'id': a.id} ) }}" title="Supprimer cet article" class="btn btn-danger"
            onclick="return confirm('Êtes-vous sûr ?')">
            <i class="fas fa-trash"></i>
        </a>
    </td>
</tr>
{% else %}
<tr>Il n'y a aucune fiche à afficher.</tr>
{% endfor %}


{% endblock %}